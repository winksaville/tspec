[package]
name = "tspec-build"
version = "0.1.0"
edition = "2024"
description = "Build script helper for tspec linker.args"

[dependencies]
toml = "0.8"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
tempfile = "3"
